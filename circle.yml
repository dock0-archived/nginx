machine:
  services:
    - docker
dependencies:
  override:
    - docker build -t test .
test:
  override:
    - docker run -ti test /bin/true
    - docker run -ti test /bin/bash --version
    - docker run -d -p 80:80 test && sleep 5
    - curl localhost
